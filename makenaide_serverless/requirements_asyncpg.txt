# 최고 경량화된 Lambda Dependencies (AsyncPG 버전)
# 총 예상 크기: ~3MB (기존 250MB에서 98.8% 감소)

# PostgreSQL 클라이언트 (최고 성능) - 사용 가능한 최신 버전
asyncpg>=0.28.0

# 비동기 HTTP 클라이언트 (pyupbit 대체) - 사용 가능한 최신 버전
aiohttp>=3.6.0

# AWS SDK (Lambda 런타임에 기본 포함되지만 명시적 버전 고정)
# boto3==1.35.9  # Lambda 런타임 기본 포함으로 제외

# Python 표준 라이브러리만 사용:
# - json (내장)
# - datetime (내장) 
# - logging (내장)
# - os (내장)
# - typing (내장)
# - asyncio (내장)

# 💡 AsyncPG 최적화 효과:
# ✅ psycopg2-binary 제거 → asyncpg (0 의존성, 3배 빠름)
# ✅ requests 제거 → aiohttp (비동기, 더 빠름)
# ✅ 모든 바이너리 호환성 문제 해결
# ✅ 완전한 비동기 처리로 성능 최적화
# ✅ Lambda Layer 크기 최소화

# 제거된 패키지들:
# - psycopg2-binary==2.9.10 (제거됨 - asyncpg로 대체)
# - requests==2.31.0 (제거됨 - aiohttp로 대체)  
# - urllib3, certifi, charset-normalizer, idna (aiohttp 의존성 해결됨)
# - pyupbit==0.2.34 (제거됨 - 직접 HTTP 요청으로 대체)
# - pandas==2.0.3 (제거됨 - 80MB+)
# - numpy==1.24.3 (제거됨 - 50MB+)
# - pandas-ta==0.3.14b0 (제거됨 - 30MB+)
# - PyJWT==2.8.0 (제거됨 - pyupbit에서만 필요했음)
# - pytz (제거됨 - pandas 종속성이었음) 